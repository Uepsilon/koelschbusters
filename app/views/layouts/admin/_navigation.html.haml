.navbar.navbar-inverse
  .container
    .navbar-header
      %a.btn.btn-default.navbar-toggle(data-target=".navbar-collapse" data-toggle="collapse")
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand(href = "#{root_path}")
        = image_tag "logo.png"
    .collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-left
        %li= link_to "Wahner KölschBusters", :root, class: :brand
        %li= link_to t('links.users'), :admin_users
        %li= link_to t('links.categories'), :admin_categories
        %li= link_to t('links.news'), :admin_news_index
        %li= link_to t('links.galleries'), :admin_galleries
        %li= link_to t('links.events'), :admin_events

      .navbar-right
        %ul.nav.navbar-nav
          %li.dropdown
            = link_to "#{current_user.name} #{content_tag(:b, '', class: :caret)}".html_safe, '#', class: "dropdown-toggle", "data-toggle" => "dropdown"
            %ul.dropdown-menu
              %li= link_to t('links.profile'), :user
              %li= link_to t('links.logout'), :destroy_user_session, method: :delete

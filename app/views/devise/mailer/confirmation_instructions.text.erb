Hallo <%= @resource.first_name %>,

bitte bestätige deine E-Mail-Adresse für deinen Account auf der Seite der
Wahner Kölschbusters.

Klick dazu einfach auf folgenden Link:
<%= confirmation_url(@resource, :confirmation_token => @token) %>
(Solltest du nicht auf den Link klicken können, so kopiere ihn bitte in die Adresszeile
deines Browsers)

Viele Grüße

Wahner Kölschbusters

